/** The single MVP shop. Later: resolve from student selection / shop membership. */
export const DEFAULT_SHOP_ID = '00000000-0000-0000-0000-000000000001';

/** Public app origin (QR targets, referral links). */
export const APP_URL = process.env.APP_URL ?? process.env.AUTH_URL ?? 'http://localhost:3000';
